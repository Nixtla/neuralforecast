------------------------------------------------------------------------

### BaseModel

> ``` text
>  BaseModel (random_seed, loss, valid_loss, optimizer, optimizer_kwargs,
>             futr_exog_list, hist_exog_list, stat_exog_list, max_steps,
>             early_stop_patience_steps, **trainer_kwargs)
> ```

Hooks to be used in LightningModule.

